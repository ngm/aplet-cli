project_name: "SuperTodo PL"
test_runner:
  name: codeception
  command: "./vendor/bin/codecept"
  arguments:
      - run
      - acceptance
      - --debug
      - --json
      - --html
      - --xml
  feature_include_switch: "-g"
